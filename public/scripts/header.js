import * as utils from './utils.js'

jQuery(function () {
    // let login = $('.submenu-item').last().children('a');
    // toggleLogin();
    // async function toggleLogin() {
    //     const userId = await utils.getUserId();
    //     console.log(userId);
    //     if (userId) {
    //         login.text('Logout');
    //         login.attr('href', '/logout');
    //     }
    // }
});